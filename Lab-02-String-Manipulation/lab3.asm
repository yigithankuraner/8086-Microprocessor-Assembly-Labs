ORG 100h

JMP START

MY_NAME   DB 'YIGITHAN KURANER'
MY_HEIGHT DB '185 CM'
MY_WEIGHT DB '100 KG'

START:

MOV AX, DS
MOV ES, AX

LEA SI, MY_NAME
MOV DI, 2000h
MOV CX, 16
CLD
REP MOVSB

LEA SI, MY_HEIGHT
MOV DI, 2010h
MOV CX, 6
CLD
REP MOVSB

LEA SI, MY_WEIGHT
MOV DI, 2020h
MOV CX, 6
CLD
REP MOVSB

MOV CX, 16
MOV SI, 2000h
ADD SI, CX
DEC SI
MOV DI, 3000h

NAME_REVERSE_LOOP:
STD
LODSB
MOV [DI], AL
INC DI
LOOP NAME_REVERSE_LOOP

MOV CX, 6
MOV SI, 2010h
ADD SI, CX
DEC SI
MOV DI, 3010h

HEIGHT_REVERSE_LOOP:
STD
LODSB
MOV [DI], AL
INC DI
LOOP HEIGHT_REVERSE_LOOP

MOV CX, 6
MOV SI, 2020h
ADD SI, CX
DEC SI
MOV DI, 3020h

WEIGHT_REVERSE_LOOP:
STD
LODSB
MOV [DI], AL
INC DI
LOOP WEIGHT_REVERSE_LOOP

MOV SI, 3000h
MOV DI, 2000h
MOV CX, 16
CLD
REP MOVSB

MOV SI, 3010h
MOV DI, 2010h
MOV CX, 6
CLD
REP MOVSB

MOV SI, 3020h
MOV DI, 2020h
MOV CX, 6
CLD
REP MOVSB

RET




