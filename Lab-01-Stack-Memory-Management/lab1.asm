ORG 100h

JMP START

SOURCE_STRING DB 'BIM303-MICROCOMPUTERS'

START:

MOV CX, 21
LEA SI, SOURCE_STRING
MOV DI, 3000h

WRITE_LOOP:
MOV AL, [SI]
MOV [DI], AL
INC SI
INC DI
LOOP WRITE_LOOP

MOV CX, 21
MOV SI, 3000h

PUSH_LOOP:
MOV AH, 0
MOV AL, [SI]
PUSH AX
INC SI
LOOP PUSH_LOOP

MOV CX, 21
MOV DI, 3020h

POP_LOOP:
POP AX
MOV [DI], AL
INC DI
LOOP POP_LOOP

RET




